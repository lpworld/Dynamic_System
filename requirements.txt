# The model uses tf.contrib and must be run on the TensorFlow 1.4 backend.
tensorflow-gpu==1.4.0
numpy==1.14.5
pandas==0.24.2
scipy==1.1.0
scikit-learn==0.20.4
pyyaml==5.4.1
matplotlib==2.2.5

# Optional, required only by the TALLRec baseline:
# torch
# transformers
